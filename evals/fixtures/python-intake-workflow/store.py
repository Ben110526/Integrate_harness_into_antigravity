from parser import parse_record


def import_batch(records, inventory):
    """Accumulate a batch only when every record is valid; return imported units."""
    raise NotImplementedError("M2: atomic validated inventory persistence")
