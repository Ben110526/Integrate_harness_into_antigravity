from store import import_batch


def intake_report(records, inventory):
    """Import records and return units imported plus sorted inventory totals."""
    raise NotImplementedError("M3: public intake summary")
